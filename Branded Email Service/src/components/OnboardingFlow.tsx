// Onboarding removed for branded-email-service customers.
// Keep a minimal placeholder export so imports don't break during builds.

export default function OnboardingFlowPlaceholder() {
  return null;
}
